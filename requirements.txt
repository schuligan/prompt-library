pydantic>=2.6
rich>=13.7
pyyaml>=6.0

# Dev / test
pytest>=8.0
ruff>=0.6

# Optional: only needed for the real-model path. The harness DEFAULTS TO A
# deterministic mock and runs fully offline without this installed. Install with:
#   pip install anthropic
# anthropic>=0.39
